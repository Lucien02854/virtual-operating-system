# VOS: Virtual Operating System
VOS is a python program that uses Tkinter and CustomTkinter to behave like an OS, there's an API and a few apps included with VOS, you can make your own apps as well with the API. You may need to install a few modules before being able to run some versions, in the releases, you can see what is required to run VOS, as well as the required python version.
